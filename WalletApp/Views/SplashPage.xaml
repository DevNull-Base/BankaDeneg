<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WalletApp.Views.SplashPage">
    <ContentPage.Content>
        <Grid BackgroundColor="{StaticResource Ink90}">
            
            <StackLayout Padding="20" Spacing="15" VerticalOptions="Center" Margin="0,30,0,0">

                <Label Style="{StaticResource Headline}" TextColor="{StaticResource Ink70}" 
                       Text="DevNull"
                       HorizontalOptions="Center" />
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>